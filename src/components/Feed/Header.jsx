import React from 'react';
import styled from 'styled-components';

function Header() {
  return <Styled.Root>여긴 헤더에용</Styled.Root>;
}

export default Header;

const Styled = {
  Root: styled.div``,
};
